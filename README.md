# CryptoCore - Спринт 3

## Спринт 3. Выполненные требования

### STR-1
- Существующая кодовая база расширена без потери функциональности
- Все тесты спринтов 1 и 2 продолжают работать
- Обратная совместимость с существующим функционалом

### STR-2
- Создан новый модуль CSPRNG: `src/csprng.py`
- Интегрирован в существующую архитектуру
- Модуль содержит функции для криптографически стойкой генерации случайных данных

### STR-3
- Обновлен README.md с документацией по новому поведению
- Примеры автоматической генерации ключей
- Информация о безопасности используемого CSPRNG
- Инструкции по запуску тестов NIST Statistical Test Suite

### CLI-1
- Аргумент `--key` стал опциональным для операций шифрования
- Поддержка обратной совместимости с существующими сценариями

### CLI-2
- При указании `--key` используется предоставленный ключ
- Сохранена полная функциональность предыдущих спринтов

### CLI-3
- При отсутствии `--key` во время шифрования:
  - Генерируется безопасный случайный 16-байтный ключ
  - Ключ выводится в stdout в hex-формате с четкой меткой
  - Шифрование продолжается с сгенерированным ключом

### CLI-4
- Для операций дешифрования `--key` остается обязательным
- При отсутствии ключа выводится ошибка и завершение работы

### CLI-5
- Реализовано обнаружение слабых ключей с предупреждениями в stderr
- Проверяются ключи: все нули, последовательные байты, повторяющиеся паттерны

### RNG-1
- Реализована выделенная функция в модуле CSPRNG
- Четкое разделение ответственности за генерацию случайных данных

### RNG-2
- Основная функция: `generate_random_bytes(num_bytes: int) -> bytes`
- Простой и понятный интерфейс для генерации случайных байтов

### RNG-3
- Используется `os.urandom()` - криптографически стойкий источник ОС
- Прямой доступ к энтропии ядра операционной системы

### RNG-4
- Запрещено использование стандартных библиотечных функций (`random`, `rand`)
- Гарантирована криптографическая стойкость генератора

### RNG-5
- Функция `generate_random_bytes` интегрирована в основную логику:
  - Генерация ключей шифрования при отсутствии `--key`
  - Генерация IV для всех режимов шифрования

### RNG-6
- Реализована обработка ошибок генерации случайных данных
- Четкие и информативные сообщения об ошибках

### KEY-1
- Автоматически генерируемые ключи всегда 16 байт для AES-128
- Соответствие стандарту шифрования

### KEY-2
- Сгенерированный ключ выводится в stdout в hex-формате
- Однократный вывод сразу после генерации

### KEY-3
- Ключ никогда не записывается в выходной файл шифротекста
- Пользователь самостоятельно сохраняет ключ из вывода терминала

### IV-1
- Генерация IV для всех режимов (CBC, CFB, OFB, CTR) использует `generate_random_bytes`
- Единообразный источник энтропии для всех случайных значений

### IV-2
- Логика работы с IV из спринта 2 полностью сохранена
- Формат файла: `<16-байтный IV><шифротекст>`
- Гибкое управление через `--iv` или чтение из файла

### TEST-1
- Реализован тест полного цикла шифрование-дешифрование с автогенерацией ключа
- Проверка вывода ключа в stdout
- Проверка успешного восстановления исходных данных

### TEST-2
- Тест уникальности 1000 сгенерированных ключей
- Проверка отсутствия дубликатов
- Статистическая проверка качества генератора

### TEST-3
- Реализована генерация тестовых данных для NIST Statistical Test Suite
- Создание файла объемом 10 МБ со случайными данными
- Подготовка данных для внешнего тестирования

### TEST-4
- Тест распределения битов для проверки энтропии
- Проверка соотношения 0/1 близкого к 50%
- Базовая проверка случайности генератора

### TEST-5
- Все тесты интероперабельности из спринта 2 продолжают работать
- Проверка совместимости с OpenSSL
- Использование ручных ключей для тестов интероперабельности

## Примеры использования

### Автоматическая генерация ключа:
```bash
cryptocore --algorithm aes --mode cbc --encrypt --input plaintext.txt --output ciphertext.bin
[INFO] Generated random key: 1a2b3c4d5e6f7890fedcba9876543210
```
### Дешифрование с автогенерированным ключом:
```bash
cryptocore --algorithm aes --mode cbc --decrypt --key 1a2b3c4d5e6f7890fedcba9876543210 --input ciphertext.bin --output decrypted.txt
```
### Обнаружение слабых ключей:
```bash
cryptocore --algorithm aes --mode cbc --encrypt --key 00000000000000000000000000000000 --input test.txt --output test.enc
Warning: The provided key appears to be weak. Consider using a randomly generated key for better security.
```
### Генерация данных для NIST STS
```bash
python tools/generate_nist_test_data.py --size-mb 10
```
### Тесты интероперабельности
```bash
python -m pytest tests/test_interoperability.py -v
```
